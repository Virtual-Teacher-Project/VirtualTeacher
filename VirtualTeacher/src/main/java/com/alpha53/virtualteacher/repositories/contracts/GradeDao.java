/*
package com.alpha53.virtualteacher.repositories.contracts;

import com.alpha53.virtualteacher.models.Course;
import com.alpha53.virtualteacher.models.Grade;
import com.alpha53.virtualteacher.models.User;

import java.util.List;

public interface GradeDao {

    List<Grade> getAll(User user, Course course);

    double getAvgGrade(User user, Course course);

    void update(Grade grade);

    void create(Grade grade);
}
*/
